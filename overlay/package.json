{
  "name": "aos-pet-overlay",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop overlay: the aos-pet capsule's ASCII pet walks on your screen",
  "main": "electron/main.cjs",
  "scripts": {
    "build": "vite build",
    "start": "vite build && electron .",
    "test": "vitest run",
    "dev": "vitest"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
